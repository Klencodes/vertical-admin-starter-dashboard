export enum EmailVerify {
    SUCCESS = 'SUCCESS',
    FAILED = 'FAILED'
}

export enum UserType {
    MERCHANT = 'MERCHANT',
    CUSTOMER = 'CUSTOMER'
}

export enum MediaTypes {
    Image = 'Image',
    Video = 'Video'
}